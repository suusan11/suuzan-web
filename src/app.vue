<script setup>
  useHead({
    titleTemplate: (title) => {
      return title ? `${title} - IDEA BOOK` : 'IDEA BOOK';
    },
  });
</script>
<template>
  <div class="l-grid">
    <Header />
    <NuxtPage />
    <Footer />
  </div>
</template>
<style>
.page-enter-active,
.page-leave-active {
  transition: all 0.3s;
}
.page-enter-from,
.page-leave-to {
  opacity: 0;
  filter: blur(1rem);
}
</style>